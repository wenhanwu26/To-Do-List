package main.ToDoList;

import java.util.ArrayList;
import java.util.stream.Stream;

public class InProgress extends EventInformation {
    public InProgress(ArrayList<Event> eventData,ToDoList toDoList) {
        super(eventData,toDoList);
    }


    // get the filtered data (already filter by school, home, other...)


}
